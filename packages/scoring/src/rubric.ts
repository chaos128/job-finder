/**
 * 이 상수가 추적하는 것은 **채점 기준**이지 출력 형식이 아니다. 다섯 축의 앵커·배점·
 * 총점 규칙이 바뀔 때만 올린다 — 그래야 `scores.rubric_version`이 다른 두 행을 두고
 * "서로 다른 기준으로 매긴 점수"라고 말할 수 있다.
 *
 * 실제로 이 브랜치에서 본문은 바뀌었다(`summary` 출력 지시가 추가됨). 그런데도 버전을
 * 올리지 않은 이유: 다섯 축의 앵커가 바이트 단위로 동일해서 v3에서 매긴 점수와 지금
 * 매기는 점수가 완전히 비교 가능하기 때문이다. 여기서 v4/v5로 올리면 운영의 168행(전부
 * v3)과 섞이는 순간 StatusStrip의 "혼재" 경고가 켜지는데, 168건 재채점은 범위 밖이라
 * 그 경고를 끌 방법이 없다 — 채점 지연을 알리는 amber 배지와 똑같이 렌더되는 거짓
 * 경고가 영구히 켜져 있으면 소유자가 amber 자체를 무시하게 된다.
 */
export const RUBRIC_VERSION = 'v4'

export const RUBRIC_AXES = ['stack', 'role', 'domain', 'growth', 'conditions'] as const
export type RubricAxis = (typeof RUBRIC_AXES)[number]

/** 축당 최대 점수. 5축 × 20 = 100. */
export const MAX_AXIS_SCORE = 20

/**
 * 루브릭 본문. `rubric.md`와 내용이 동일해야 한다 — 이 파일을 고치면 `rubric.md`도 같이 고칠 것.
 *
 * 왜 런타임에 `rubric.md`를 읽지 않고 문자열로 인라인했는가: 원래 구현은
 * `readFileSync(join(dirname(fileURLToPath(import.meta.url)), 'rubric.md'))`였다.
 * vitest(소스에서 직접 실행)에서는 통과하지만, Next.js 프로덕션 빌드에서는 실패한다.
 * webpack이 `import.meta.url`을 빌드 시점의 절대 경로 문자열로 그대로 박아 넣고,
 * `.md` 파일은 정적으로 추적되지 않아 배포 산출물(`.nft.json` 파일 추적, Vercel 등)에
 * 포함되지 않는다. 로컬에서 `next build && next start`로 재현: 응답은 200이지만,
 * `packages/scoring/src` 디렉터리가 없는 실제 배포 환경을 흉내 내면
 * `ENOENT: no such file or directory, open '.../packages/scoring/src/rubric.md'`로 500이 난다.
 */
const RUBRIC_MARKDOWN = `# 채점 루브릭 v4

공고 1건을 5개 축으로 채점한다. 각 축 0~20점, 합계 0~100점.
**반드시 한 번에 한 건만 채점한다.** 여러 건을 한꺼번에 보면 앞 공고가 뒤 공고의 기준점이 된다.

각 축은 아래 앵커에 맞춘다. 앵커 사이는 보간한다 (예: 5점, 15점).

## stack — 기술 스택 적합도

**자격 요건(필수)에 적힌 기술만 센다.** 회사 소개나 "우리는 이런 기술을 씁니다" 같은
본문의 기술 언급은 이 축의 근거가 아니다 — 회사가 React를 쓴다는 사실과 이 자리가
React를 요구한다는 사실은 다르다. 필수 요건이 다섯 줄이면 그중 몇 줄이 이력서의
주력 스택과 겹치는지로 읽는다.

- **0점** 필수 요건이 주력 스택과 거의 겹치지 않는다
- **10점** 필수 요건의 절반 정도 겹치고 나머지는 학습 가능한 범위다
- **20점** 필수 요건 대부분이 이력서의 주력 스택이다

우대 사항은 필수 요건보다 가볍게 본다. 우대에만 걸치면 가점 근거가 되지 못한다.

## role — 역할·직급 적합도

**직무·연차·책임 범위를 함께** 본다. 셋 중 하나만 맞으면 20점이 될 수 없다.

- **0점** 직무가 다르거나, 요구 연차·기대 역할이 명백히 어긋난다
- **10점** 범위에는 걸치나 일부 불일치가 있다
- **20점** 직무·연차·책임 범위가 정확히 맞는다

**직무를 먼저 본다.** 필수 요건의 중심 직무가 프론트엔드가 아니면 연차와 직급이
아무리 맞아도 **5점을 넘지 않는다.** 판별은 자격 요건의 무게로 한다 — "백엔드 N년
이상"이 필수 첫 줄이고 프론트엔드가 "경험 있음" 한 줄이면 이 공고는 백엔드 공고다.
테크리드·풀스택처럼 여러 영역을 같은 비중으로 요구하는 자리도 마찬가지다. 프로필의
기피 조건("프론트엔드 외 영역까지 겸하는 풀스택")이 이 판단의 근거다.

연차는 "하한을 넘겼는가"가 아니라 **"이 팀이 몇 년차를 찾고 있는가"**로 읽는다.
검색 필터로 연차를 걸러도 실제 JD는 훨씬 낮은 연차를 겨냥한 경우가 많다.
프로필 경력이 하한을 크게 웃돌면 형식상 지원은 되지만 팀이 찾는 대상이 아니다.

- 신입·주니어를 명시하거나 신입 기준 연봉을 제시하면 **0~3점**.
  본문 어디에 있든(자격요건, 연봉 안내, 채용 배경) 같은 신호로 취급한다.
- 요구 하한이 **3년 이하**면 미들급을 찾는 공고다 — **8점을 넘기지 않는다.**
- 하한이 5년 이상이고 리드·챕터·기술 표준 수립 같은 책임이 함께 적혀 있으면 15점 이상.
- 연차 명시가 없으면 기대 역할 범위로만 판단하고, 그마저 모호하면 10점으로 둔다.

## domain — 도메인 적합도

- **0점** 경험 없는 도메인이고 기존 경험의 이전성도 낮다
- **10점** 인접 도메인이라 경험 일부가 이전된다
- **20점** 직접 경험한 도메인이다

## growth — 회사의 성장성

**회사가 얼마나 성장하고 있는가**를 본다. 지원자가 무엇을 배울 수 있는지가 아니다.
낯선 분야라 배울 게 많다는 것은 이 축의 가점 사유가 **아니다** — 그건 domain에서 이미 감점된다.

- **0점** 성장이 정체됐거나 축소 중으로 보인다
- **10점** 안정적이지만 뚜렷한 성장 신호가 없다
- **20점** 매출·사용자·투자·시장 지위에 명확한 성장 신호가 있다

근거로 삼을 것: 매출·MAU·거래액 증가율, 투자 유치 단계, 시장 점유율,
해외 진출, 채용 규모 확대. 공고에 이런 신호가 전혀 없으면 **10점**으로 둔다
— 정보가 없는 것을 성장이 없는 것으로 읽지 않는다.

## conditions — 근무 조건

- **0점** 프로필에 적힌 선호 조건과 충돌한다
- **10점** 무난하지만 특별한 이점은 없다
- **20점** 위치·회사 규모·조건이 선호와 부합한다

## 출력 형식

\`breakdown\`의 키는 정확히 \`stack\`, \`role\`, \`domain\`, \`growth\`, \`conditions\` 다섯 개다.
각 값은 0~20 정수이고, \`total\`은 다섯 값의 합과 **정확히 일치**해야 한다.
\`reasoning\`은 점수의 근거를 2~4문장으로 적는다. 축별로 왜 그 점수인지 드러나야 한다.

\`summary\`는 **이 공고가 어떤 일인지** 약 세 문장으로 적는다(400자 이내).
채점 근거가 아니라 공고 자체의 요약이다 — 회사·제품이 무엇을 하는지, 이 역할이
구체적으로 무슨 일을 다루는지, 이 포지션이나 팀에서 눈에 띄는 점은 무엇인지를
담는다. "잘 맞는다/안 맞는다"는 \`reasoning\`의 몫이지 여기가 아니다.
`

export function loadRubric(): string {
  return RUBRIC_MARKDOWN
}
